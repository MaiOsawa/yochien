<h1>幼稚園情報一覧</h1>
<table class="table table-striped">
    <tr>
        <th>名称</th>
        <th>詳細</th>
    </tr>
    
    <% @facilities.each do |facility| %>
    
    <tr>
        <td><%= facility.name %></td>
        <td>
            <%= link_to "詳細", facility_path(facility), class: "btn btn-info" %>
        </td>
    </tr>
    
    <% end %>

</table>

<%= link_to "戻る", facilities_path, class: "btn btn-primary" %>